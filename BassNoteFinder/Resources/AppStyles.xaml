<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <SolidColorBrush x:Key="AppBackgroundBrush" Color="#0B1118"/>
    <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#141E29"/>
    <SolidColorBrush x:Key="PanelBorderBrush" Color="#2B3A4A"/>
    <SolidColorBrush x:Key="PanelHeaderBrush" Color="#F2F6F8"/>
    <SolidColorBrush x:Key="MutedTextBrush" Color="#AAB8C5"/>
    <SolidColorBrush x:Key="SubtleTextBrush" Color="#718397"/>
    <SolidColorBrush x:Key="PrimaryBrush" Color="#159B9A"/>
    <SolidColorBrush x:Key="SuccessBrush" Color="#D99A36"/>
    <SolidColorBrush x:Key="SecondaryBrush" Color="#334659"/>
    <SolidColorBrush x:Key="FocusBorderBrush" Color="#65E0D5"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#65E0D5"/>
    <SolidColorBrush x:Key="ModeHeaderBorderBrush" Color="#35566A"/>
    <SolidColorBrush x:Key="StatusPillBackgroundBrush" Color="#101923"/>
    <SolidColorBrush x:Key="CheckBoxGlyphBrush" Color="#172432"/>
    <SolidColorBrush x:Key="CheckBoxBorderBrush" Color="#52657A"/>
    <SolidColorBrush x:Key="CheckBoxHoverBrush" Color="#203344"/>
    <SolidColorBrush x:Key="CheckMarkBrush" Color="#071114"/>
    <SolidColorBrush x:Key="ButtonHoverBorderBrush" Color="#8DF5EC"/>
    <SolidColorBrush x:Key="ButtonTextBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="TeacherCardBackgroundBrush" Color="#172532"/>
    <SolidColorBrush x:Key="TeacherCardBorderBrush" Color="#38667A"/>
    <SolidColorBrush x:Key="StudentCardBackgroundBrush" Color="#1C2630"/>
    <SolidColorBrush x:Key="StudentCardBorderBrush" Color="#5A5138"/>
    <SolidColorBrush x:Key="WarmAccentBrush" Color="#F0BE67"/>
    <SolidColorBrush x:Key="CorrectBrush" Color="#32CD32"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="#FF4500"/>
    <SolidColorBrush x:Key="CorrectFeedbackBackgroundBrush" Color="#173D27"/>
    <SolidColorBrush x:Key="ErrorFeedbackBackgroundBrush" Color="#4B201D"/>
    <LinearGradientBrush x:Key="FretboardWoodBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#241208" Offset="0"/>
        <GradientStop Color="#5C3118" Offset="0.35"/>
        <GradientStop Color="#3B1E0F" Offset="0.7"/>
        <GradientStop Color="#1E0E06" Offset="1"/>
    </LinearGradientBrush>

    <Style x:Key="CardBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource PanelBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Margin" Value="7"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>

    <Style x:Key="ControlBarBorderStyle" TargetType="Border" BasedOn="{StaticResource CardBorderStyle}">
        <Setter Property="Padding" Value="12,10"/>
        <Setter Property="Margin" Value="15,0,15,15"/>
    </Style>

    <Style x:Key="ModeOptionsBorderStyle" TargetType="Border" BasedOn="{StaticResource CardBorderStyle}">
        <Setter Property="Padding" Value="14,10"/>
        <Setter Property="Margin" Value="6,0,6,4"/>
    </Style>

    <Style x:Key="ModeHeaderBorderStyle" TargetType="Border" BasedOn="{StaticResource CardBorderStyle}">
        <Setter Property="Padding" Value="18,13"/>
        <Setter Property="Margin" Value="7,0,7,12"/>
        <Setter Property="BorderBrush" Value="{DynamicResource ModeHeaderBorderBrush}"/>
    </Style>

    <Style x:Key="StatusPillBorderStyle" TargetType="Border">
        <Setter Property="BorderBrush" Value="{DynamicResource PanelBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="10,5"/>
        <Setter Property="Background" Value="{DynamicResource StatusPillBackgroundBrush}"/>
    </Style>

    <Style x:Key="PanelTitleTextStyle" TargetType="TextBlock">
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{DynamicResource PanelHeaderBrush}"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,13,0,6"/>
    </Style>

    <Style x:Key="EyebrowTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <Style x:Key="PageTitleTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource PanelHeaderBrush}"/>
        <Setter Property="FontSize" Value="38"/>
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>

    <Style x:Key="StatusTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}"/>
        <Setter Property="Margin" Value="0,2,0,0"/>
        <Setter Property="TextAlignment" Value="Left"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="MinHeight" Value="24"/>
    </Style>

    <Style x:Key="ToolbarCheckBoxStyle" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource PanelHeaderBrush}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="6,4"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Border x:Name="Root" Background="Transparent" CornerRadius="6" Padding="6,4">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="Glyph" Width="15" Height="15" CornerRadius="4"
                                    Background="{DynamicResource CheckBoxGlyphBrush}"
                                    BorderBrush="{DynamicResource CheckBoxBorderBrush}" BorderThickness="1"
                                    VerticalAlignment="Center"/>
                            <Path x:Name="Check" Grid.Column="0" Margin="3" Stroke="{DynamicResource CheckMarkBrush}" StrokeThickness="2"
                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round" Opacity="0"
                                  Data="M 1 5 L 4.5 8.5 L 11 1.5"/>
                            <ContentPresenter Grid.Column="1" Margin="7,0,0,0" VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Glyph" Property="Background" Value="{DynamicResource AccentBrush}"/>
                            <Setter TargetName="Glyph" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            <Setter TargetName="Check" Property="Opacity" Value="1"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{DynamicResource CheckBoxHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Opacity" Value="0.42"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ToolbarLabelTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="8,4,4,4"/>
    </Style>

    <Style x:Key="ActionButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="{DynamicResource ButtonTextBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="MinHeight" Value="36"/>
        <Setter Property="Padding" Value="12,7"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            CornerRadius="8"
                            SnapsToDevicePixels="True">
                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.92"/>
                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource ButtonHoverBorderBrush}"/>
                            <Setter TargetName="ButtonBorder" Property="BorderThickness" Value="1"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.78"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource FocusBorderBrush}"/>
                            <Setter TargetName="ButtonBorder" Property="BorderThickness" Value="1"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.48"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PrimaryActionButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
        <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
    </Style>

    <Style x:Key="SuccessActionButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
        <Setter Property="Background" Value="{DynamicResource SuccessBrush}"/>
    </Style>

    <Style x:Key="SecondaryActionButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
        <Setter Property="Background" Value="{DynamicResource SecondaryBrush}"/>
    </Style>
</ResourceDictionary>
